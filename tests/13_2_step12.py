""" 
Юный математик 🤓🌶️
Дима учится в седьмом классе и сейчас они проходят обыкновенные дроби с натуральными числителем и знаменателем.
Вчера на уроке математики Дима узнал, что дробь называется правильной, если ее числитель меньше знаменателя, 
или несократимой, если нет равной ей дроби с меньшими натуральными числителем и знаменателем.

Дима очень любит математику, поэтому дома он долго экспериментировал, 
придумывая и решая разные задачки с правильными несократимыми дробями.
Одну из этих задач Дима предлагает решить вам с помощью компьютера.

На вход программе подается натуральное число n. 
Напишите программу, которая находит наибольшую правильную несократимую дробь с суммой числителя и знаменателя, 
равной n.

Формат входных данных
На вход программе подается натуральное число n.

Формат выходных данных
Программа должна вывести ответ на задачу.

Примечание.
Возможно, вам потребуется функция gcd(), которая позволяет находить наибольший общий делитель (НОД) двух чисел. 
Функция реализована в модуле math.
"""

from math import gcd

n = int(input())

# Ищем максимальную дробь num / (n - num), где num < n - num и gcd(num, n - num) = 1
# Чтобы дробь была наибольшей, числитель должен быть как можно ближе к n/2
for num in range((n - 1) // 2, 0, -1):
    den = n - num
    if gcd(num, den) == 1:
        print(f"{num}/{den}")
        break

""" 
Как это работает
Дробь $\frac{num}{den}$ правильная, если num < den. Так как num + den = n, получаем den = n - num и условие num < n - num, то есть num < n/2.
Чтобы дробь была наибольшей, числитель должен быть как можно ближе к n/2 — тогда дробь ближе к единице.
Идём от (n-1)//2 вниз и берём первую пару, у которой gcd(num, den) = 1 (несократимая).
gcd == 1 гарантирует, что дробь несократима.
"""
